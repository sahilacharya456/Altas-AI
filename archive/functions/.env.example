GEMINI_API_KEY=your-gemini-api-key

# Optional legacy Firebase Functions config equivalent.
# Prefer Firebase Secret Manager for new deployments; legacy Runtime Config is deprecated.
# firebase functions:config:set google.api_key="your-gemini-api-key"
# firebase functions:secrets:set GEMINI_API_KEY

# Server-side only. Never expose Gemini/OpenAI-compatible keys in Expo env vars.
