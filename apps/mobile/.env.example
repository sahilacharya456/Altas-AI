EXPO_PUBLIC_FIREBASE_API_KEY=your-firebase-web-api-key
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=000000000000
EXPO_PUBLIC_FIREBASE_APP_ID=1:000000000000:web:0000000000000000000000

# AltasAI backend API. Local default works for web, iOS simulator, and Android emulator.
EXPO_PUBLIC_ALTASAI_API_BASE_URL=http://localhost:3001

# Android physical devices cannot use localhost for the desktop backend.
# Use your computer LAN IP, for example:
# EXPO_PUBLIC_ALTASAI_API_BASE_URL=http://192.168.1.25:3001
# Or use a tunnel URL from ngrok/cloudflared when LAN routing is blocked.

# Do not add Gemini, OpenAI, or other paid AI provider keys here.
# Mobile clients must call the backend API; AI keys stay server-side only.

# Firebase App Check is setup-only until provider credentials are configured.
# Do not add server App Check secrets to the mobile env.

# Crash reporting (optional). No real DSN belongs in this example.
# EXPO_PUBLIC_SENTRY_DSN=https://xxx@yyy.ingest.sentry.io/zzz

# Stripe Checkout is created by backend/api. Do not add Stripe secret keys here.
