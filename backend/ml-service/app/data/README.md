# AltasAI ML Data

- `raw`: local raw exports or anonymized source files.
- `processed`: generated cleaned features.
- `synthetic_seed`: small hand-written samples used to bootstrap models.

Do not store private user data in this repository. Use anonymized examples only.
